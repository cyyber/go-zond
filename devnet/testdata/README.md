# Unsafe development wallet

`unsafe-development-wallet.seed` is a public, development-only ML-DSA-87
credential. It is committed so disposable local networks and end-to-end suites
use the same funded account without runtime wallet state.

Never fund or use this seed outside disposable local development networks.
